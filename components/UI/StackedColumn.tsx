import Stack from '@mui/material/Stack';
import { styled } from '@mui/material/styles';

const StackedColumn = styled(Stack)(({ theme: _theme }) => ({
  height: '100%',
  width: '100%'
}));

export default StackedColumn;
